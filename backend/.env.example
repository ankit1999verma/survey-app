PORT=5000
JWT_SECRET=supersecretgpkeysurveyorjwt
NODE_ENV=development

# Database Configuration
# Supported dialects: sqlite, postgres, mysql
DB_DIALECT=sqlite

# SQLite settings (default)
DB_STORAGE=./database.sqlite

# MySQL / PostgreSQL settings (uncomment and fill if using DB_DIALECT=postgres or mysql)
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_NAME=gp_survey
